# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently **empty** apart from `README.md`. Per the README, it is a repository for Unity-related experiments and practice (`Unity 관련 실험 및 실습 저장소`). No Unity project, source code, build configuration, or tooling exists yet.

When Unity content is added, update this file with the concrete build/test/run commands and architecture for the project that lands here. Until then, there are no project-specific commands to document.

## Conventions

- Commit messages in this repository are written in Korean (e.g. `프로젝트 README 파일 추가`). Match that style.
- The `main` branch is the integration branch; feature work happens on separate branches and is merged via PR.
